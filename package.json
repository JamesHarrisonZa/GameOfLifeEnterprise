{
  "name": "gameoflifetypescript",
  "version": "1.0.0",
  "author": "James Harrison",
  "private": true,
  "scripts": {
    "build": "tsc",
    "browserify": "browserify -r ./dist/app.js:App -o ./dist/bundle.js",
    "test": "karma start",
    "_test-cover": "istanbul cover node_modules/jasmine/bin/jasmine.js JASMINE_CONFIG_PATH=jasmine.json --print none",
    "test-summary": "npm run _test-cover && remap-istanbul -i coverage/coverage.json -o coverage/coverage-remaped-summary.txt -e \\src\\ -t text-summary && type coverage\\coverage-remaped-summary.txt",
    "test-detail": "npm run _test-cover && remap-istanbul -i coverage/coverage.json -o coverage/coverage-remaped.txt -e \\src\\ -t text && type coverage\\coverage-remaped.txt",
    "test-report": "npm run _test-cover && remap-istanbul -i coverage/coverage.json -o coverage/lcov-report-remapped -e \\src\\ -t html && start coverage/lcov-report-remapped/index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jasmine": "^2.5.54",
    "browserify": "^14.4.0",
    "istanbul": "^0.4.5",
    "jasmine": "^2.7.0",
    "karma": "^1.7.1",
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^1.1.0",
    "karma-typescript": "^3.0.5",
    "remap-istanbul": "^0.9.5",
    "tslint": "^5.6.0",
    "tslint-immutable": "^4.0.2",
    "typescript": "^2.4.2"
  }
}
